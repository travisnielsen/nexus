{
  "name": "dataagent-ag-ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix src/frontend",
    "dev:debug": "npm run dev:debug --prefix src/frontend",
    "dev:ui": "npm run dev:ui --prefix src/frontend",
    "dev:agent": "npm run dev:agent --prefix src/frontend",
    "build": "npm run build --prefix src/frontend",
    "start": "npm run start --prefix src/frontend",
    "lint": "npm run lint --prefix src/frontend",
    "install:all": "npm install --prefix src/frontend"
  }
}
