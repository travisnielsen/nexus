# Azure AI Foundry Configuration\nAZURE_AI_PROJECT_ENDPOINT=https://your-project.services.ai.azure.com/api/projects/your-project-name\nAZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4o-mini\n\n# Recommendations Agent (A2A) URL
# The A2A agent provides flight recommendations via Agent-to-Agent protocol
RECOMMENDATIONS_AGENT_URL=http://localhost:5002

# MCP Data Server Configuration
# The MCP server provides flight data via HTTP REST API
MCP_SERVER_URL=http://localhost:8001
MCP_ENABLED=true
MCP_TIMEOUT=10.0
MCP_CLIENT_ID=<mcp-server-client-id>  # Same as MCP server's client ID
MCP_AUTH_ENABLED=false  # Set to true to enable token acquisition for MCP server

# Entra ID Authentication
# Use the same values as the frontend app registration, or create a separate API registration
AZURE_AD_CLIENT_ID=your-client-id
AZURE_AD_TENANT_ID=your-tenant-id

# Authentication (enabled by default)
# Set to "false" to bypass Azure AD authentication entirely
# WARNING: Do NOT use AUTH_ENABLED=false in production environments!
AUTH_ENABLED=true

# Agent Framework Logging Level
# Controls verbosity of agent_framework and agent_framework_ag_ui libraries
# Options: DEBUG, INFO, WARNING, ERROR (default: WARNING)
# AGENT_FRAMEWORK_LOG_LEVEL=WARNING

# =============================================================================
# OpenTelemetry Observability Configuration
# =============================================================================

# Enable OpenTelemetry instrumentation (set to "true" to enable)
ENABLE_INSTRUMENTATION=false

# Telemetry mode: "appinsights" (default) or "otlp"
# - appinsights: Send telemetry to Azure Application Insights (production)
# - otlp: Send telemetry to OTLP endpoint (local dev with Aspire/Jaeger/Tempo)
TELEMETRY_MODE=appinsights

# Azure Monitor connection string (required for TELEMETRY_MODE=appinsights)
# Get this from Azure Portal > Application Insights > Connection String
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=...;IngestionEndpoint=...

# OTLP exporter endpoint (required for TELEMETRY_MODE=otlp)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# Optional: Enable console output for debugging (useful for local development)
# ENABLE_CONSOLE_EXPORTERS=true

# Optional: Log prompts and responses (use with caution - contains sensitive data)
# ENABLE_SENSITIVE_DATA=true
