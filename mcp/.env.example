# MCP Server Configuration
# Copy this file to .env and fill in the values

# Server settings
MCP_HOST=0.0.0.0
MCP_PORT=8001

# Authentication (Entra ID / Azure AD)
# Set AUTH_ENABLED=true to require JWT tokens on API requests
AUTH_ENABLED=false
AZURE_AD_TENANT_ID=
AZURE_AD_CLIENT_ID=
# Optional: Custom App ID URI (defaults to api://<client-id>)
# AZURE_AD_APP_ID_URI=
